# Brainstormed TODOs

- Pane management: toggle single/split view; explore dynamic pane count (up to four) with intuitive layout controls.
- File handling: keep .txt/.md support; remember last open directory; add recent-file history; auto-reload when a file changes on disk.
- Editor polish: basic Markdown syntax highlighting; word wrap; configurable font and size; toggle whitespace visibility.
- Split layouts: support 4 pane arrangements and add Ctrl+Tab focus cycling so documents can be switched without touching the mouse.
- Saving workflow: autosave interval; confirm-on-overwrite; surface quick-save location/history; warn before closing dirty panes.
- UX niceties: keyboard shortcuts for pane switching/toggling; status indicators for active pane/file type; per-pane rename option. Keyboard shortcuts for the most common Markdown commands. Markdown edit and preview views
- Future integrations: clipboard history; export to PDF/HTML for Markdown; optional spell-check or linting plug-ins.
